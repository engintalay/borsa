enum inputTypes {
  input = "input",
  textarea = "textarea",
  submit = "submit",
  button = "button",
  checkbox = "checkbox",
  clearButton = "clearButton"
}

enum formMethods {
  post = "POST",
  get = "GET",
}
